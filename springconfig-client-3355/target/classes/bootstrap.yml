#系统级配置, 服务启动时环境配置
spring:
  cloud:
    config:
      name: application          #github资源名称, 不需要加.yml后缀名
      #profile: test               #加载github资源中的dev配置到本地
      label: config               #github分支名称
      uri:
      - http://localhost:3366/    #config服务器地址